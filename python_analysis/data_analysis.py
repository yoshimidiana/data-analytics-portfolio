"""\nA simple Python data analysis script.\n\nThis script demonstrates how to load a CSV file using pandas, generate basic summary statistics, and create a simple bar chart using matplotlib.\n\nUsage:\n1. Place your CSV file in a data/ directory or provide a path to the file.\n2. Update the `data_file` variable in the __main__ section with the path to your CSV file.\n3. Replace 'column_name' in `plot_distribution` with a numeric column from your dataset to visualize its distribution.\n\nRun the script from the command line:\n```
python data_analysis.py
```\n"""\n\nimport pandas as pd\nimport matplotlib.pyplot as plt\n\ndef load_data(filepath: str) -> pd.DataFrame:\n    """Load dataset from a CSV file and return a pandas DataFrame."""\n    return pd.read_csv(filepath)\n\n\ndef analyze_data(df: pd.DataFrame) -> None:\n    """Print the first five rows and summary statistics of the DataFrame."""\n    print(df.head())\n    print("\nSummary statistics:")\n    print(df.describe())\n\n\ndef plot_distribution(df: pd.DataFrame, column: str) -> None:\n    """Create a bar chart for the specified column's distribution."""\n    plt.figure()\n    df[column].value_counts().plot(kind='bar')\n    plt.title(f'Distribution of {column}')\n    plt.xlabel(column)\n    plt.ylabel('Count')\n    plt.tight_layout()\n    plt.show()\n\n\nif __name__ == "__main__":\n    # Example usage: update 'data/sample_data.csv' to your dataset path\n    data_file = 'data/sample_data.csv'\n    df = load_data(data_file)\n    analyze_data(df)\n    # Replace 'column_name' with a column from your dataset to plot\n    plot_distribution(df, 'column_name')\n
